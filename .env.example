# Local development needs no secrets: the D1 binding is declared in wrangler.jsonc
# and scheduled scraping runs inside the worker itself.
# Optional override for the stream sealing secret:
# STREAM_SECRET=change-me
